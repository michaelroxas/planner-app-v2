  <p>
    <strong>Category:</strong>
    <%= @category.task %>
  </p>

  <%= link_to 'Edit', edit_category_path(@category) %> |
  <%= link_to 'Back', categories_path %>

  <h2>Add a task:</h2> 

<%# render form  %>
<%= render 'todos/form' %>

<%# render tasks %>
<table>
<%= render 'todos/todos' %>
</table>